" Bash's `type` builtin formats things using four space indents, so follow
" that lead.
set expandtab
set shiftwidth=4
set softtabstop=4
set tabstop=4

" Mark whitespace.
set list listchars=tab:>\ ,trail:-
